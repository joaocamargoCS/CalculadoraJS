var num = parseInt(
  prompt("Digite um número para saber se o mesmo é negativo ou positivo")
);

if (num < 0) {
  alert(num + " é um número negativo");
} else if (num > 0) {
  alert(num + " é um número positivo");
}
